{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/build/${relativeFileDirname}/${fileBasenameNoExtension}.o",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Compile",
    },
  ],
  "compounds": []
}